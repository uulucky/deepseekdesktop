# 隐私与广告说明

适用版本：0.3.4。维护者：uulucky；联系邮箱：489583561@qq.com。

这是第三方桌面客户端，不是 DeepSeek 官方产品。软件使用 DeepSeek 官方服务以及维护者提供的广告与更新服务；不同服务适用各自的隐私条款。

## 什么保存在本机

Mac 默认数据目录为 `~/Library/Application Support/DeepSeek Desktop/`，不在 `.app` 或 DMG 内；Chromium 登录会话使用其中的用户数据存储。Mac 上 Chromium 可能使用系统钥匙串保护 Cookie；ad-hoc 签名不能保证跨版本钥匙串身份稳定，更新后可能弹出授权或要求重新登录。

- 便携模式的数据在程序旁的 `data/`：`harness/` 保存会话、工具记录和凭据，`chromium/` 保存网页登录状态、Cookie、缓存与网页存储，`config/` 保存偏好和账户信息缓存，`logs/` 保存日志。
- **API Key 由 Harness 保存在本地凭据文件，可能是明文。** POSIX 文件权限不等于 Windows 加密；本软件没有把它包装成加密保险箱。有权读取数据目录的本机用户、管理员或恶意程序可能获取这些数据。
- 登录网页的会话信息会持久保存；客户端还会在内存中读取平台令牌以查询余额、用量及管理 Key。不会索要你把密码交给维护者。
- “网页版（免费）”使用同一个专用持久化 DeepSeek 会话分区，保存官方网页的 Cookie、缓存、站点存储和登录态。客户端不读取或注入该页面的登录令牌，不把网页版会话转换成本地 Harness 会话。
- 对话、工具输出和日志可能包含个人信息、文件路径或文件内容。普通日志对常见 Key、Bearer 令牌和 URL token 做脱敏，但并不能识别所有形式的秘密。提交反馈前仍须检查。

## 什么会联网

中文行动摘要由当前模型在正常对话请求中生成；客户端不把思考原文另发给翻译服务，不为旧记录自动发起补译请求。摘要与原文随本地会话保存。折叠只改变显示方式，不删除原文，也不是保密措施。提示词与摘要计入正常模型 Token。

发送消息时，提示词、上下文、附件及工具返回的相关内容可能发送给当前配置的模型服务商。文件保存在本机不代表其内容永不离开本机。命令、浏览器、MCP 或其他用户配置工具还可能访问其目标站点，尤其在 Full Access 模式下。

登录、余额、用量、Key 管理与充值连接 DeepSeek 官方平台；充值由官方页面及其支付服务处理，客户端维护者不代收款，也不接收支付凭证。账号网页还可能加载其自身的验证码、资源或支付域名。

用户选择“网页版（免费）”或启动时恢复上次的网页模式时，客户端加载 `https://chat.deepseek.com/`。在该模式中，消息、附件、搜索查询、朗读/分享操作及账号数据直接由 DeepSeek 官方网页处理，适用 DeepSeek 的条款和隐私规则。客户端只提供隔离窗口、持久登录和显隐切换，不代理消息，也不采集网页正文。为兼容官方页面，网页版 User-Agent 去掉 Electron 和客户端产品标识，但仍发送真实操作系统类型与底层 Chromium 版本；客户端不伪造账号、不处理验证码，也不改写 Cookie。切回工作台后页面会被隐藏而不是刷新，因此官方网页可继续保持内存状态和网络连接；完全退出软件才结束该页面进程。

新版恢复机制在网页可见且主页面返回 429 后，等待服务器指定时间（默认 60 秒）自动重试一次，再次拒绝后暂停自动请求。等待截止时间保存在本地偏好中，重启不能提前重试。错误恢复仅绕过 HTTP 缓存，不清除 Cookie、站点存储或会话。诊断日志记录状态码、缓存标记和重试时间，不记录请求头、网页登录令牌或网页正文。

完整域名、请求目的和频率见 [网络连接说明](NETWORK.md)。运行本地服务本身不需要把对话上传至 uulucky。

## 广告与维护者的服务

软件左侧有广告位，这是产品的一部分，目前没有关闭或编辑广告的开关。客户端启动时及约每 30 分钟读取 `https://www.uulucky.com/dsad.json`，从 `https://img.uulucky.com` 加载图片；广告文字、图片和跳转链接可以由维护者远程更换，不代表软件功能或官方背书。广告不能向客户端注入脚本；图片来源被限制到指定 HTTPS 域名。点击广告会打开目标网站，适用目标网站的隐私规则。

客户端代码没有主动在广告/更新请求中附加 API Key、DeepSeek Cookie、对话内容或稳定的设备追踪 ID；未集成行为统计或自动崩溃上传 SDK。但服务端/CDN 仍会接收到网络请求的 IP、时间、路径、User-Agent 等常规元数据，并可能按托管服务配置保留访问日志。**本说明不承诺这些服务端元数据零收集或零留存。**

更新每小时检查一次版本清单，Windows 用户点击后才下载并安装；Mac 用户点击后由系统浏览器下载 DMG，需要退出并手动替换应用。更新请求连接维护者的 OSS/CDN。数字签名和构建来源的作用及局限见 [安全说明](SECURITY.md)。

## 管理与删除

Mac 替换或删除“应用程序”中的 `.app` 不删除 Application Support 中的记录；如需彻底删除本地数据，应先 Cmd+Q 退出并备份，再仅删除“关于”里确认的本软件数据目录。不要删除整个 Application Support 或其他软件目录。

“退出登录”清除平台会话和部分账户缓存，**不等于撤销 API Key，也不会删除本地对话或 Harness 中配置的 Key**。如需停用 Key，请到 DeepSeek 官方平台撤销。

关闭软件后，可以备份或删除自己的数据目录。删除前确认没有需要保留的对话；新版默认工作目录在 `data/workspace/`，其中也可能有你创建的文件。自动更新设计为保留 `data/`，重要数据仍建议自行备份。分享便携包时请使用官方原始压缩包，不要分享你已经运行过的整个文件夹。

无法使用便携目录时会回退到 Electron 用户数据目录；实际位置可以在“设置 → 关于 → 数据目录”查看。卸载或删除程序文件不一定自动删除回退目录，也不会删除用户自选工作目录的文件。

本说明描述客户端实现，不代替 DeepSeek、支付服务、GitHub 或阿里云的隐私政策。它也不是独立安全审计报告。
